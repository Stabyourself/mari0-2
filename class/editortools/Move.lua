local Move = class("Editor.Move")

function Move:initialize(editor)
    self.editor = editor
end

function Move:select()
    
end

function Move:unSelect()
    
end

function Move:update(dt)
    
end

function Move:draw()
    
end

function Move:mousepressed(x, y, button)
    
end

function Move:mousereleased(x, y, button)
    
end

return Move
