local Pick = class("Editor.Pick")

function Pick:initialize(editor)
    self.editor = editor
end

function Pick:select()
    
end

function Pick:unSelect()
    
end

function Pick:update(dt)
    
end

function Pick:draw()
    
end

function Pick:mousepressed(x, y, button)
    
end

function Pick:mousereleased(x, y, button)
    
end

return Pick
