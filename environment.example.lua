PROF_CAPTURE = false

return {
    hitBoxDebug = false,
    quadDebug = false,
    stencilDebug = false,
    inputDebug = false,
    portalVectorDebug = false,
    memoryDebug = false,

    noEnemies = false,
    musicDisabled = false,
    volume = 1,
    scale = 3,

    vsync = true,

    ffKeys = {
        {
            key="+",
            val=30
        },

        {
            key="-",
            val=0.1
        }
    },
}