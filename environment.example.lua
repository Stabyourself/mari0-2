return {
    physicsDebug = false,
    quadDebug = false,
    stencilDebug = true,
    physicsDebug = false,
    inputDebug = true,

    noEnemies = false,
    musicDisabled = false,
    volume = 0,
    scale = 3,

    vsync = true,

    ffKeys = {
        {
            key="+",
            val=30
        },

        {
            key="-",
            val=0.1
        }
    },
}